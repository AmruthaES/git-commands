git clone <url> => create the repository to our local workspace

git status => to check whether git has initialzed in ur current directory

git add <filename> => add the  file"filename" to staging area(to perfome committ and push)

git add .=> add all the files in the directory to the staging area

git commit -m "massage" => to commit that it has been tracked by git

git push -u origin main => to push the commited file to the globel workspace

git initilize git in a folder